## Resolución de Algoritmos
Se usó el comando
    gem install time_diff

## TweetSec
Se utilizó la API de Twitter, se creó una app para conseguir los API Keys y los Tokens secret, los cuales están en un archivo .env
